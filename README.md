
  # Create one page landing page

  This is a code bundle for Create one page landing page. The original project is available at https://www.figma.com/design/1HAV7IL90FCrht27xfOTbI/Create-one-page-landing-page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  